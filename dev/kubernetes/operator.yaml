image:
  repository: k3d-mlflow-registry.localhost:12000/mlflow-operator/mlflow-operator
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: dev

defaultModelImage: k3d-mlflow-registry.localhost:12000/mlflow-operator/mlflow:latest

resources: {}
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi
